let square;
let circle;
let pipe;
function setup(){
    new Canvas(800, 400);
    background(250);
    world.gravity.y = 1

    square = new Sprite(800 / 2, 400 / 2, 25, 25, "static");
    circle = new Sprite(100, 350, 25, "dynamic");
    pipe = new Group();
}
function draw(){
    while(i === 1);
        //do a action
    for(let i; i === 1; i++);
        //do a action
    if (mouse.presses("left")){
        
    }
        
}
